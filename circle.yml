machine:
  environment:
      ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
  override:
      - ./gradlew dependencies

test:
  override:
      - ./gradlew testDebug lint

general:
  artifacts:
    - sqlitehelper/build/reports/tests/debug/

